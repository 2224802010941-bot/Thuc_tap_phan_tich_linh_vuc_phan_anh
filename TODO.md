# TODO

## Nav/layout (templates/layout.html)
- [ ] Mở `templates/layout.html` và thay đổi giao diện `<nav>` để:
  - Căn đẹp hơn, spacing thống nhất, dễ nhìn
  - Các link không xuống dòng (whitespace-nowrap)
  - Hiển thị trạng thái active rõ ràng theo `request.path`
  - Giữ nguyên responsive hiện tại (không thêm hamburger)
- [ ] Test bằng cách mở các route: `/submit`, `/monitor`, `/dashboard`, `/maps`, `/admin` (kiểm tra active state + căn lề)

